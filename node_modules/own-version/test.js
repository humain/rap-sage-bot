var ownVersion = require('./index');


console.log('sync', ownVersion.sync());

ownVersion(function (err, version) {
  console.log('async', err, version);
});

console.log('sync', ownVersion.sync());

